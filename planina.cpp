#include <iostream>
using namespace std;

int main(){
    int interations, result_i, result_f;
    cin>>interations;
    if(interations == 1){
        result_i = (1 * 2)+1;
        result_f = result_i * result_i;
    }else if(interations == 2){
        result_i = (2 * 2)+1;
        result_f = result_i * result_i;
    }else if(interations == 3){
        result_i = (2 * 2 * 2)+1;
        result_f = result_i * result_i;
    }else if(interations == 4){
        result_i = (2 * 2 * 2 * 2)+1;
        result_f = result_i * result_i;
    }else if(interations == 5){
        result_i = (2 * 2 * 2 * 2 * 2)+1;
        result_f = result_i * result_i;
    }else if(interations == 6){
        result_i = (2 * 2 * 2 * 2 * 2 * 2)+1;
        result_f = result_i * result_i;
    }else if(interations == 7){
        result_i = (2 * 2 * 2 * 2 * 2 * 2 * 2)+1;
        result_f = result_i * result_i;
    }else if(interations == 8){
        result_i = (2 * 2 * 2 * 2 * 2 * 2 * 2 * 2)+1;
        result_f = result_i * result_i;
    }else if(interations == 9){
        result_i = (2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2)+1;
        result_f = result_i * result_i;
    }else if(interations == 10){
        result_i = (2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2)+1;
        result_f = result_i * result_i;
    }else if(interations == 11){
        result_i = (2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2)+1;
        result_f = result_i * result_i;
    }else if(interations == 12){
        result_i = (2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2)+1;
        result_f = result_i * result_i;
    }else if(interations == 13){
        result_i = (2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2)+1;
        result_f = result_i * result_i;
    }else if(interations == 14){
        result_i = (2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2)+1;
        result_f = result_i * result_i;
    }else if(interations == 15){
        result_i = (2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2 * 2)+1;
        result_f = result_i * result_i;
    }

    cout<<result_f;
return 0;
}
